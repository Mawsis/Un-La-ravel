<?php

namespace App\Http\Controllers;

// The application's base controller. PostController, UserController and
// CommentController extend it, mirroring a real Laravel app. The controller
// extractor emits one Controller node per named class regardless of base class
// (ADR 0006), so this class is itself extracted (FQN
// App\Http\Controllers\Controller) with an empty action list — it has no public
// methods and no route references it.
abstract class Controller
{
    //
}
